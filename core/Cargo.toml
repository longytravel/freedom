[package]
name = "freedom-core"
version = "0.1.0"
edition = "2021"
description = "Rust backtest engine core for the Freedom forex platform."
license = "MIT OR Apache-2.0"

[lib]
name = "freedom_core"
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
